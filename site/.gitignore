node_modules
dist
.tanstack
.DS_Store
*.local

# Generated on demand by `npm run qr` with the real Pages URL
public/wedding-qr.svg

# Note: src/routeTree.gen.ts is auto-generated by the TanStack Router vite
# plugin but intentionally committed — `npm run build` type-checks before
# vite runs the generator, so the file must already exist on a clean checkout.

# Local env (API keys etc.) — never commit
.env.local
*.local
